[phases.setup]
nixPkgs = ["python3", "gcc", "postgresql_16.dev"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "gunicorn a_core.wsgi:application --bind 0.0.0.0:$PORT"
